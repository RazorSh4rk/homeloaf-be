service: homeloaf-be
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  deploymentMethod: direct
  region: us-west-2
  stage: production
  memorySize: 512
  timeout: 10
  environment:
    OPENAPI_KEY: ${param:OPENAPI_KEY}

functions:
  homeloaf:
    handler: a  pp.handler
